import { createContext, FC, useState } from "react";

const defaultValue = {};
const {{contextName}}Context = createContext(defaultValue);

const {{contextName}}Provider: FC = ({children}) => {
	const [state, setState] = useState(defaultValue)

	return <{{contextName}}Context.Provider value={state}>
		{children}
	</{{contextName}}Context.Provider>
}

export default {{contextName}};
