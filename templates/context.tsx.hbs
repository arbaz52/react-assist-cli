import { createContext, FC, useState } from "react";

const defaultValue = {};
const {{contextName}} = createContext(defaultValue);

const {{contextName}}Provider: FC = ({children}) => {
	const [state, setState] = useState(defaultValue)

	return <{{contextName}}.Provider value={state}>
		{children}
	</{{contextName}}.Provider>
}

export default {{contextName}};
